define(function() {
	var cookie = require("rfl").cookie;
	describe('Test rfl.cookie', function(){
		it("Should can save and get the value through key.", function(){
			cookie.set("test", "lala");
			expect(cookie.get("test")).toEqual("lala");
		});
	});
});